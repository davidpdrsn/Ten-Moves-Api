%div.stats
  %h1 Stats
  %dl
    %dt
      Number of moves:
    %dd
      = @dashboard.total_moves_count

%div.top-list
  %h1 Top list
  %dl
  - if @dashboard.empty?
    There are no moves
  - else
    %ul
      - @dashboard.top_list.each do |move|
        %li{"data-id" => 1}
          = move
